nanocyte-credentials:
  container_name: "nanocyte-credentials"
  build: ${HOME}/Projects/Octoblu/nanocyte-credentials-service
  ports:
   - "80"

  labels:
    traefik.port: "80"

  dns: "192.168.99.1"

  external_links:
    - redis-tmp:redis

  volumes:
    - /usr/src/app/node_modules
    - ${HOME}/Projects/Octoblu/nanocyte-credentials-service:/usr/src/app

  env_file:
    - ./nanocyte-credentials.env
