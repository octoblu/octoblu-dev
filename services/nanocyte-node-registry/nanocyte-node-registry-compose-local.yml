nanocyte-node-registry:
  container_name: nanocyte-node-registry
  build: ${HOME}/Projects/Octoblu/octoblu-dev/services/_local
  privileged: true

  ports:
   - "$LOCAL_PORT:$SERVICE_PORT"

  labels:
    traefik.port: "80"

  env_file:
    - ./nanocyte-node-registry-local.env
